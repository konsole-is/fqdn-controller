apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "fqdn-controller.fullname" . }}-serving-cert
  labels:
  {{- include "fqdn-controller.labels" . | nindent 4 }}
spec:
  dnsNames:
  - '{{ include "fqdn-controller.fullname" . }}-webhook-service.{{ .Release.Namespace
    }}.svc'
  - '{{ include "fqdn-controller.fullname" . }}-webhook-service.{{ .Release.Namespace
    }}.svc.{{ .Values.kubernetesClusterDomain }}'
  issuerRef:
    kind: Issuer
    name: '{{ include "fqdn-controller.fullname" . }}-selfsigned-issuer'
  secretName: webhook-server-cert
