apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "fqdn-controller.fullname" . }}-networkpolicy-viewer-role
  labels:
  {{- include "fqdn-controller.labels" . | nindent 4 }}
rules:
- apiGroups:
  - fqdn.konsole.is
  resources:
  - networkpolicies
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - fqdn.konsole.is
  resources:
  - networkpolicies/status
  verbs:
  - get
